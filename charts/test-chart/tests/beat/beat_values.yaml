beats:
  test-beat:
    name: test-beat
    namespace: test-namespace
    version: "8.15.0"
    type: filebeat
    elasticsearchRef:
      name: test-es
    config:
      filebeat.inputs:
        - type: container
          paths:
            - /var/log/containers/*.log
