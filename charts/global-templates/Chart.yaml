apiVersion: v2
name: global-templates
description: A library chart that provides a set of reusable templates for Helm Charts
type: library
version: 1.16.0
dependencies:
  - name: common
    version: 2.27.0
    repository: https://charts.bitnami.com/bitnami
